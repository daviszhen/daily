services:
  smart-daily:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9871:9871"
    env_file: .env
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
